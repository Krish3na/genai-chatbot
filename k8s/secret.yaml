apiVersion: v1
kind: Secret
metadata:
  name: genai-chatbot-secrets
  namespace: genai-chatbot
type: Opaque
stringData:
  # Add your OpenAI API key here (not base64 encoded)
  # Replace with your actual key when deploying
  OPENAI_API_KEY: "REPLACE_WITH_YOUR_ACTUAL_KEY" 