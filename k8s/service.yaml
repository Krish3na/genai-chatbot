apiVersion: v1
kind: Service
metadata:
  name: genai-chatbot-service
  namespace: genai-chatbot
  labels:
    app: genai-chatbot
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: genai-chatbot 